class Book {
  int id;
  String title;
  String author;
  String description;
  double price;
  String image;

  Book(this.id, this.title, this.author, this.description, this.price,
      this.image);
}
